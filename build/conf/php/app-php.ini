[PHP]
; Send un-interceptable PHP errors (fatals, bootstrap/parse errors) to the
; container stderr stream. display_errors stays Off so they never render to users.
log_errors = On
error_log = /proc/self/fd/2

[Date]
date.timezone = UTC
